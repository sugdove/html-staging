const { Router } = require('express')

function deepClone(target) {
  const cloneObj = new target.constructor()
}
const arr = [11, 2, 12, 1, 3, 13]

function sort1(arr) {
  return arr.sort((a, b) => {
    return a - b
  })
}
//冒泡
function sort2(arr) {
  for (let i = 0; i < arr.length - 1; i++) {
    for (let j = 0; j < arr.length; j++) {
      if (arr[j] > arr[j + 1]) {
        const temp = arr[j + 1]
        arr[j + 1] = arr[j]
        arr[j] = temp
      }
    }
  }
  return arr
}
console.log(sort2(arr))

.stop .prevent .keycode  .sync .native .self .once .trim .lazy(离开光标调用) .number

admin 123456 token axios token code: 实现    ['user', 'list_user_manage'] 映射表: {user: {
  button: false,
  add: true，

} }

['user', 'list_user_manage']

routes = [
  {
    code: 'list_user_manage'
  },
]
动态路由
Router.beforeEach((req, res, next) => {

})

封装比较有挑战的组件

webpack plugin 实现

bundle chunk

设置cookies 302状态码 301

浏览器线程：

业务：工业互联网、设备 检测工业设备
      开发流程：可自己估时间
